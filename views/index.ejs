<h1>Recently Added</h1>
<div>
    <% books.forEach(book=>{ %>
        <a href="/books/<%= book.id %>">
            <img height="150" width="150" src="<%= book.coverImagePath %>">
        </a>
    <% }) %> 
</div>